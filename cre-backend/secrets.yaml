# ============================================================
# VaultSentinel - CRE Secrets Configuration
# ============================================================
# Secrets referenced here are resolved from the .env file
# during simulation, or from the CRE Vault during production.
# ============================================================
secrets:
  etherscan_api_key:
    env_var: ETHERSCAN_API_KEY
  # Add additional API keys as needed:
  # alchemy_api_key:
  #   env_var: ALCHEMY_API_KEY
  # infura_api_key:
  #   env_var: INFURA_API_KEY
